# Screenshot Evidence (SP0)

Expected files:
- `tenant-selector-header.png` (Dashboard header with TenantSelector visible)
- `report-page-route.png` (`/reports` route rendered)

Capture guidance:
1. Start app: `pnpm dev`
2. Login as non-super-admin user.
3. Open dashboard and capture header screenshot.
4. Navigate to `/reports` and capture page screenshot.
5. Save PNG files in this folder with exact names above.
