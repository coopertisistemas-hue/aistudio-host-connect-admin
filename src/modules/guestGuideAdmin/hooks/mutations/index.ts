/**
 * Phase 0 mutation hook placeholder.
 * No write hooks are implemented before mutation-path decision.
 */

export const guestGuideAdminMutationStatus = {
  allowed: false,
  reason: "Phase 0 feasibility gate not approved",
} as const;
